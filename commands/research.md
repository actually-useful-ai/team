---
name: research
description: "Just the facts: recon (internal codebase map) + scout (external prior art, market context). No verdicts, no opinions. Usage: /team:research [path]"
arguments:
  - name: input
    description: "Optional path to focus on. Defaults to cwd."
    required: false
---

Invoke the `team` skill with `--research` scope: $ARGUMENTS
