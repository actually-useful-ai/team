---
name: skeptics
description: "Just the skeptics committee: adversarial attack on a pitch + pre-mortem + kill criteria. Use when you have a draft pitch and want it stress-tested. Usage: /team:skeptics [path or pitch text]"
arguments:
  - name: input
    description: "Optional path or pitch text to attack. Defaults to cwd."
    required: false
---

Invoke the `team` skill with `--skeptics` scope: $ARGUMENTS
